// console.log("000008999999923".length);

$('#merchantId').on('keyup', function () {
    if (this.value.length == 15) {
        console.log(this.value);
    }
    //retrive data to database or send to route and controller

});


